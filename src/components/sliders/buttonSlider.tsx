import React from "react";
import { Text, View } from "react-native";

export const buttonSlider = () => {
  return (
    <View>
      <Text>Button Slider Component</Text>
    </View>
  );
};

export default buttonSlider;
